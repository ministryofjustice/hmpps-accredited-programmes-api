# SAR (Custody) – QAT Review Response Pack

**Date:** 3 July 2026
**Author:** Raby Whyte
**Ticket context:** Follow-up to the QAT (Roxanne Stephenson) review of the "Accredited Programmes in Custody" SAR report.
**Owner going forward:** Raby (taking over from Nia Evans).

---

## 0. Very important scoping note (READ FIRST)

The email chain refers to the **"Accredited Programmes in Custody" SAR report**. Custody-side SAR data (course, course_participation, offering, pni_result, oasys_pni_result, organisation, staff, sexual_offence_details, eligibility_override_reason, audit_record, referral, person, referral_status_history, referral_status_reason, selected_sexual_offence_details) is generated by the sibling repo:

> **`hmpps-accredited-programmes-api`** (aka the "custody API")
> – `src/main/kotlin/.../service/SubjectAccessRequestService.kt`
> – `src/main/resources/sar_template.mustache`

It is **NOT** produced by this repo (`hmpps-accredited-programmes-manage-and-deliver-api`), which produces its own separate SAR ("Manage and Deliver – Accredited Programmes") for the community/delivery side (referrals, programme groups, attendees, availability, delivery-location preferences, etc.).

All findings below therefore relate to the custody API repo at `/Users/raby.whyte/code/hmpps-accredited-programmes-api` on the current `origin/main` (HEAD `d492b3c5` – "Add organisation to SAR", 1 July 2026).

Nia's Two SAR-adding PRs are already **merged to `origin/main`**:
- `#1079` "Add Staff to SAR" (77153f08)
- `#1080` "Add organisation to SAR" (d492b3c5)

---

## 1. Verification of every row in Roxanne's 3 July table

Table columns key:
- **In SAR code?** = actually included in the `Content` data class + `sar_template.mustache` on `origin/main` today.
- **Verdict** = what we should tell QAT.

| Entity / DD Row | Roxanne's note (3 July) | In SAR code? | Verdict / action |
|---|---|---|---|
| audience | Not to be included | No | ✅ Correct. Nothing to do. |
| audit_record | No issues. "Referrer" & "Audit Username" present. | Yes – `SarAuditRecord` (referrerUsername, auditUsername, statusFrom/To, courseName, courseLocation, auditAction, auditDateTime, prisonNumber) | ✅ Correct. |
| course | Now shows the name of the course but no further details | Yes – `SarCourse { name }` only | ✅ **By design.** Only `name` is exposed per the DD. Roxanne is happy. |
| course_participation | "No Data Held" – unable to check | Yes – full `SarCourseParticipation` (14 fields incl. courseName, source, type, outcome, yearStarted/Completed, location, createdBy/updatedBy, detail) | ⚠️ **Needs QAT retest with a prisoner who has known course participation records.** Code is present; "No Data Held" just means the test PRN had none in dev. |
| course_variant | Not to be included | No | ✅ Correct. |
| eligibility_override_reason | "Reason" and "override type" are Yes on DD. Which column is authoritative – G ("not SAR") or H ("in API Endpoint")? | **No** – `EligibilityOverrideReasonEntity` exists in the codebase (used by `ReferralService`), but is **not** in `SubjectAccessRequestService.Content` and not in the SAR template. | ⚠️ **DD needs correcting.** Column G ("not SAR") is authoritative. Column H should be updated to **No** for the SAR API endpoint. Note the possible source of QAT confusion: the referral SAR row includes a field called `referrerOverrideReason` (rendered as "Referrer override reason") – this is a free-text field on the referral itself, **not** the `eligibility_override_reason` reference table. Different thing. |
| flyway_schema_history | Not to be included | No | ✅ Correct (framework/infrastructure table). |
| oasys_pni_result | On report as "No Data Held". DD said "unused empty table – do not include". DD now updated. In SAR API – Yes. Confirm? | **Yes** – `SarOasysPniResult { pniResultId, prisonNumber, oasysAssessmentId, programmePathway }` and rendered under `<h2>OASys PNI results</h2>`. | ✅ **Confirmed: it IS in the SAR API/report.** PNI data is sourced from the ARNS team. If the prod table is currently empty, the report will show "No Data Held". Recommend the DD's SAR column stays as **Yes** (keeps us covered when data starts flowing). The old "do not include in SAR" DD note should be removed. |
| offering | Not to be included | No (only used indirectly to resolve organisation via `filteredReferrals.mapNotNull { it.offering?.organisationId }`) | ✅ Correct. |
| offering_old | Not to be included | No | ✅ Correct. |
| organisation | To be included ("Name" Yes on DD) | **Yes** – added by PR #1080 (1 July). `SarOrganisation { id, code, name, gender }`. Template renders only **Name** (per DD). | ✅ **Now included.** QAT should re-review the latest dev report generated after PR #1080. |
| Person | No issues. Location = "Released" if sentence served. | Yes – `SarPerson` (14 fields incl. `location`, `gender`, all release date fields, `sentenceType`, `indeterminateSentence`). | ✅ Correct. Location resolution behaviour understood. |
| pni_result | "Oasys assessment ID" **not** on report | Partial – `SarPniResult` exposes 11 fields but **not** `oasysAssessmentId`. However `PniResultEntity` DOES have `val oasysAssessmentId: Long?` (line 22 of the entity). | 🐞 **Genuine code gap.** `oasysAssessmentId` is on the underlying entity and (per DD) should be on the SAR. Needs a small change to `SarPniResult`, its `toSarPniResult()` mapper, and the mustache template. Also update `sar-api-response.json` / `sar-expected-render-result.html` test fixtures. See §3 below. |
| pni_rule | Not to be included | No | ✅ Correct. |
| prerequisite | Not to be included | No | ✅ Correct. |
| referral | No issues | Yes – `SarReferral` (15 fields) | ✅ Correct. |
| referral_status | Not to be included | No | ✅ Correct (reference/lookup data). |
| referral_status_category | Not to be included | No | ✅ Correct. |
| referral_status_history | No issues | Yes – `SarReferralStatusHistoryEntity` (11 fields) | ✅ Correct. |
| referral_status_reason | No issues | Yes – `SarReferralStatusReason` (5 fields, distinct-by-code, only reasons actually seen in the person's history) | ✅ Correct. |
| referrer_user | Not to be included | No (only `referrerUsername` string is surfaced on `SarReferral` / `SarAuditRecord`) | ✅ Correct. |
| selected_sexual_offence_details | No issues. Table currently empty but referenced in solution – left in. | Yes – `SarSelectedSexualOffenceDetails { id, referralId, sexualOffenceDetailsId }` | ✅ Correct. |
| sentence_category | Not to be included | No | ✅ Correct. |
| sexual_offence_details | No issues | Yes – `SarSexualOffenceDetails { id, category, description, score }` (only records referenced by that person's `selected_sexual_offence_details`) | ✅ Correct. |
| staff | To be included ("Last name" Yes on DD) | **Yes** – added by PR #1079 (1 July). `SarStaff { staffId, firstName, lastName, primaryEmail, username, accountType }`. Template renders only **Last name** (per DD). | ✅ **Now included.** QAT should re-review the latest dev report. |

---

## 2. Summary of things QAT/Business need to know

1. **Two brand-new sections are now in SAR (as of 1 July)**: `Organisation` and `Staff`. If Roxanne's latest sample report was generated before these merges, she needs a fresh report from dev.
2. **`oasys_pni_result` IS in the SAR API** – DD note "do not include" is out of date and should be removed. Report shows "No Data Held" only because the table is empty in prod today.
3. **`eligibility_override_reason` is NOT in the SAR** – DD column H should be changed from Yes to No. QAT should not confuse it with the free-text `referrer_override_reason` field on the referral (which IS in SAR under "Referrer override reason").
4. **`course` intentionally shows Name only** – matches the DD.
5. **`course_participation` "No Data Held"** is a test-data issue, not a code issue. We need a PRN with course-participation records to prove it out. (Suggest we ask QAT to nominate one, or we pick one from prod audit records and re-generate the dev report.)

---

## 3. Only genuine code fix required: add `oasysAssessmentId` to `SarPniResult`

**Repo:** `hmpps-accredited-programmes-api`

Three files to change:

### 3.1 `SubjectAccessRequestService.kt`
Add field to the DTO and mapper:
```kotlin
data class SarPniResult(
  // ...existing fields...
  val oasysAssessmentId: Long?,   // NEW
  val pniResultJson: String?,
  val basicSkillsScore: Int?,
)

private fun List<PniResultEntity>.toSarPniResult(): List<SarPniResult> = map {
  SarPniResult(
    // ...existing fields...
    oasysAssessmentId = it.oasysAssessmentId,  // NEW
    pniResultJson = it.pniResultJson,
    basicSkillsScore = it.basicSkillsScore,
  )
}
```

### 3.2 `src/main/resources/sar_template.mustache`
Under `<h2>PNI results</h2>`, add one row (suggested placement: just after "OASys assessment completed date" for coherence):
```mustache
<tr><td>OASys assessment ID</td><td>{{ optionalValue oasysAssessmentId }}</td></tr>
```

### 3.3 Test fixtures
Update:
- `src/test/resources/sar/sar-api-response.json`
- `src/test/resources/sar/sar-expected-render-result.html`
- `SubjectAccessRequestServiceTest.kt` – add `oasysAssessmentId` to the expected `SarPniResult` object.

### 3.4 Effort
- ~30 min work + PR review.
- No DB migration needed (field already on `PniResultEntity` / underlying view).
- Small, low-risk change.

---

## 4. Open questions to raise on the call with SAR change management

1. **Timing of QAT's most recent sample report** – was it generated from a dev deploy that includes PR #1079 (Staff) and PR #1080 (Organisation)? If not, they're comparing an older report.
2. **`course_participation` retest** – can QAT nominate a PRN with known course-participation history so we can prove it out end-to-end?
3. **DD housekeeping** – can we get sign-off on the following DD corrections (all documented above)?
   - `eligibility_override_reason` column H → **No**
   - `oasys_pni_result` – remove "do not include in SAR" annotation; leave SAR API column as **Yes**
4. **`pni_result.oasysAssessmentId`** – confirm QAT expectation, then we ship the small fix in §3 and re-generate the report for review.
5. **Deadline** – the original deadline of 30 June has already slipped. What is the new agreed target date, given the fix + retest cycle above?

---

## 5. Draft reply for Kiril / David / SAR change management

> **OFFICIAL**
>
> Hi David, Roxanne,
>
> Happy to get on a call — Kiril, over to you to set one up.
>
> Ahead of the call, I've done a full code-level walk-through of the SAR service in `hmpps-accredited-programmes-api` (the custody codebase that generates this report) and can confirm the following against Roxanne's 3 July table:
>
> 1. **Organisation and Staff** are now in the SAR – both were merged to `main` on 1 July (PRs #1079 and #1080). If the latest sample report Roxanne reviewed was generated before that date, please could we re-run it on dev so QAT has a like-for-like comparison?
>
> 2. **`oasys_pni_result`** – confirming this **is** included in the SAR API today (fields: pniResultId, prisonNumber, oasysAssessmentId, programmePathway). The DD annotation "unused empty table – do not include in SAR" is out of date and can be removed; the SAR API column should stay as **Yes**. It will show "No Data Held" while the prod table is empty, which is the correct behaviour.
>
> 3. **`eligibility_override_reason`** – the DD column G ("not SAR") is correct: this reference table is not in the SAR API. We'd recommend updating column H to **No** as well, to avoid the mismatch. Please note there is a separate free-text field called *Referrer override reason* which lives on the referral itself – that one **is** in the SAR (under the Referral section) and shouldn't be confused with the eligibility_override_reason reference table.
>
> 4. **`pni_result` – "OASys assessment ID" missing** – confirmed: this field exists on the underlying entity but is not currently mapped into the SAR DTO or template. This is a genuine gap. It's a small, low-risk change (~30 min plus PR) which I can raise straight after the call.
>
> 5. **`course_participation` showing "No Data Held"** – the code is fully in place (all 14 DD fields are mapped). We just need a prisoner number with known course-participation history to prove it out. Could QAT nominate one from prod, or shall we pick one?
>
> 6. **`course`** – confirming that surfacing the course name only is the intended behaviour per the DD.
>
> All other items in Roxanne's table match the code exactly – no further action needed there.
>
> Thanks,
> Raby

---

## 6. References (file paths for the call)

Custody API (`/Users/raby.whyte/code/hmpps-accredited-programmes-api`), `origin/main` HEAD `d492b3c5`:

- Service: `src/main/kotlin/uk/gov/justice/digital/hmpps/hmppsaccreditedprogrammesapi/service/SubjectAccessRequestService.kt`
- Template: `src/main/resources/sar_template.mustache`
- PniResultEntity (has `oasysAssessmentId`): `src/main/kotlin/.../domain/entity/view/PniResultEntity.kt` (line 22)
- Recent SAR PRs on main: #1079 (Staff), #1080 (Organisation), #1071 (SAR refactor), #1061 (SAR field additions)

For completeness, the *separate* Manage-and-Deliver SAR (community-side, not part of this QAT review):
- Service: `.../accreditedprogrammesmanageanddeliverapi/service/SubjectAccessRequestService.kt`
- Template: `src/main/resources/sar_template.mustache` (header: "Manage and Deliver – Accredited Programmes")

